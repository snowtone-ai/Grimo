# Superseded Carol source references

These files were previously exposed as active `approved-3d` authority. They are retained byte-for-byte for provenance and comparison, but do not override the current manifest at `assets/grimo/source/carol/approved-3d/authority.json`.

The archived hoof module is not current hoof authority.
